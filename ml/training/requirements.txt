# Core
transformers>=4.51.0
accelerate>=1.0.0
peft>=0.13.0
datasets>=3.0.0
torch>=2.5.0

# Recommended for speed (use one of these)
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git  # Best for local Mac + cloud
# or
llama-factory>=0.9.0

# Evaluation
openai>=1.30.0          # or litellm for multi-provider
tqdm
pandas

# Optional but recommended
wandb
flash-attn>=2.7.0; platform_system != "Darwin"  # Not needed on Mac
